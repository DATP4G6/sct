class Citizen(int townId) {
    decorator randomDeath {
        if (rand() < 0.005) {
                destroy;
        }
    }

    decorator randomDeath {
        if (rand() < 0.01) {
                destroy;
        }
    }
}

function Setup() -> void {}
